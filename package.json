{
  "name": "khud",
  "version": "0.3.0",
  "description": "Cross-agent identity compiler for Claude Code, OpenCode, and Cursor",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist",
    "README.md"
  ],
  "bin": {
    "khud": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "link": "npm run build && npm link",
    "prepack": "npm run build",
    "test": "npm run build && node --test tests/capture-finalize.test.mjs tests/repair-regressions.test.mjs tests/session-note-dedupe.test.mjs tests/prompt-echo-guard.test.mjs tests/context-preview.test.mjs tests/context-sync.test.mjs",
    "test:focused": "npm run build && node --test tests/repair-regressions.test.mjs"
  },
  "keywords": [
    "ai",
    "claude",
    "cursor",
    "opencode",
    "developer-tools",
    "automation"
  ],
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
